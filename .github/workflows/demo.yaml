on: push

jobs:
  first_job:
    runs-on: ubuntu-latest

    steps:
      - name: welcome message
        run: echo "my first github action"

      - name: list the files
        run: ls -ltr

      - name: read file
        run: cat README.md
