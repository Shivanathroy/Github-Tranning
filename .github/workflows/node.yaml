name: Hello World

on:
  workflow_dispatch:

jobs:
  Hello:
    runs-on: ubuntu-latest

    steps:
      - name: Hello Steps
        run: |
          echo "Welcome to GitHub"
          echo " welocome code"
