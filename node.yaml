name: Basic CI Pipeline

on: [push]

jobs:
  test-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Print Hello
        run: echo "Hello Shiva - CI Working!"