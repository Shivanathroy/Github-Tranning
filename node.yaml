name: Node Build
name: CI Pipeline

on:
push:
branches: [ "main" ]

jobs:
build:
runs-on: ubuntu-latest

```
steps:
  - name: Checkout Code
    uses: actions/checkout@v3

  - name: deploy
    run: echo "Deplying Application"
    name: Build Docker Image
    run: docker build -y myapp .
    name: run docker containers
    run: docker run -d -p 3000:3000 my-app
    

  
